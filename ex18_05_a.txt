// 회전운동 (p5.Vector 클래스 미사용)

let theta = 0.0, r = 250.0;

function setup() {
  createCanvas(500, 500);
  background(0);
}

function draw() {
  let x = width/2 + r * cos(theta);
  let y = height/2 + r * sin(theta);
  ellipse(x, y, 30, 30);
  theta += 0.05;
}