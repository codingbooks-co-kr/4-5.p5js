// 랜덤하게 생성되는 원으로 특정한 위치의 원형 만들기

function setup() {
  createCanvas(300, 300);
  background(0);
}

function draw() {
  let x = random(width);
  let y = random(height);
  let d = dist(width/2, height/2, x, y);
  if (d < width/2) {
    fill(255, 255, 0);
    ellipse(x, y, 10, 10);
  }
}