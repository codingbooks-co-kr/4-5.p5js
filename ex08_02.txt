// 마우스의 현재 위치 좌표에서 사방으로 랜덤하게 퍼지는 선

function setup() {
  createCanvas(300, 300);
}

function draw() {
  background(0);
  for (let i = 0; i < 100; i++) {
    stroke(255);
    let x = random(width);
    let y = random(height);
    line(mouseX, mouseY, x, y);
  }
}