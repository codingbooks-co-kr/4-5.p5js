// 가로선 그리기1

let x1 = 50;
let x2 = 250;
let y = 60;

function setup() {
  createCanvas(300, 300);
  background(200);

  line(x1, y, x2, y);
  line(x1, y*2, x2, y*2);
  line(x1, y*3, x2, y*3);
  line(x1, y*4, x2, y*4);
}