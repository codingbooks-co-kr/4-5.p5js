// 나선운동

let theta = 0.0, r = 0.0;

function setup() {
  createCanvas(300, 300);
  background(200);
}

function draw() {
  let x = width/2 + r * cos(theta);
  let y = height/2 + r * sin(theta);
  ellipse(x, y, 30, 30);
  theta += 0.05;
  r += 0.1;
}