// 배열과 반복문을 사용하여 세 개의 문자를 오른쪽으로 이동

let c = ['a', 'b', 'c'];
let x = [-50, 0, 50];

function setup() {
  createCanvas(500, 300);
}

function draw() {
  background(0);
  fill(255);
  textSize(50);
  for (let i = 0; i < 3; i++) {
    text(c[i], x[i], (80*i)+80);
    x[i]++;
  }
}