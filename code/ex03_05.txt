// 원을 점점 크게 그리기

let x = 150.0, y = 150.0, d = 60.0;

function setup() {
  createCanvas(300, 300);
  background(200);

  noFill();
  ellipse(x, y, d, d);
  ellipse(x, y, d*2, d*2);
  ellipse(x, y, d*3, d*3);
  ellipse(x, y, d*4, d*4);
}