// noise() 함수를 사용하여 랜덤 파형 만들기1

let t = 0.0;

function setup() {
  createCanvas(500, 200);
}

function draw() {
  background(0);
  stroke(255);
  noFill();
  beginShape();
  for (let x = 0; x <= width; x++) {
    let y = 50 + 100 * noise(t);
    vertex(x, y);
    t += 0.01;
  }
  endShape();
}