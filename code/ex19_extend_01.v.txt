let array = [10, 20, 30, 40];

function setup() {
  result = array.reduce(function (acc, cur, index, array) { 	// 콜백함수(익명함수 표현)
    print(acc, cur, index, array);
    return acc + cur;
  }, 0);
  print(result);
}