// mousePressed() 대신 시스템 변수인 mouseIsPressed를 사용

let x = 0, y = 0, d = 50;

function setup() {
  createCanvas(500, 500);
  background(0);
}

function draw() {
  noStroke();
  fill(255, 255, 0);
  ellipse(x, y, d, d);
  if (mouseIsPressed) {
    x = mouseX;
    y = mouseY;
  }
}