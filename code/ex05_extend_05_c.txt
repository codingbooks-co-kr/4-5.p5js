// 한 가지 이상의 경우를 선택할 때는 반드시 if문만으로 구성

function setup() {
  createCanvas(300, 300);
}

function draw() {
  background(0);
  fill(255, 255, 0);
  if (mouseY > 0) {
    rect(0, 0, width, height/3);
  }
  if (mouseY > height*1/3) {
    rect(0, height/3, width, height/3);
  }
  if (mouseY > height*2/3) {
    rect(0, height*2/3, width, height/3);
  }
}