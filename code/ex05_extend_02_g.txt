// ❶ 논리 오류

let x = 0; 

function setup() {
  createCanvas(300, 300);
}

function draw() {
  let velocity = 1;
  background(0);
  ellipse(x, height/2, 30, 30);
  x += velocity;
  if(x > width) {
    velocity *= -1;
  }
}