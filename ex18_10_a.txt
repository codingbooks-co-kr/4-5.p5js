// 불규칙 운동 (p5.Vector 클래스 미사용)

let x, y, tx = 0.0, ty = 0.5;

function setup() {
  createCanvas(500, 500);
  background(0);
}

function draw() {
  x = noise(tx) * width;
  y = noise(ty) * height;
  ellipse(x, y, 30, 30);
  tx += 0.01;
  ty += 0.01;
}