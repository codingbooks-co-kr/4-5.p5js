let sh, c, s;

function setup() {
  print(this);		// this는 전역 객체인 window 객체를 가리킴
  sh = new Shape();
  c = new Circle();
  s = new Square();
}

class Shape {
  constructor() {		// 생성자
    print(this);		// this는 new 연산자로 생성할 객체를 가리킴
  }
}

class Circle extends Shape { }
class Square extends Shape { }