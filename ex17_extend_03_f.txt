// “클래스” 기반 객체 생성

function setup() {
  // new 연산자로 객체 생성
  c = new Circle();

  print(c.c, c.x, c.y, c.d, c.velocity);
  print(typeof c);
}

class Circle {		// 클래스
  constructor() {
    this.c = 255;
    this.x = width / 2;
    this.y = height / 2;
    this.d = 30;
    this.velocity = 1;
  }
}