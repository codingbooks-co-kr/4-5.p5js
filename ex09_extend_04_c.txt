// 배열의 크기는 동적으로 변경 가능

let array = [];

function setup() {
  print(array.length);	// 배열크기: 0
  array[0] = 10;	// 배열요소 추가
  array[1] = 20;	// 배열요소 추가
  print(array.length);	// 배열크기: 2

  array[2] = 30;	// 배열요소 추가
  print(array.length);	// 배열크기: 3
}