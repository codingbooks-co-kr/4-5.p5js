// 원을 두 개 생성

function setup() {
  createCanvas(300, 300);
}

function draw() {
  background(0);
  display(100.5, 100.5, 255, 120.5);
  display(200.5, 200.5, 150, 120.5);
}

function display(x, y, c, d) {
  fill(c, 0, 0);
  ellipse(x, y, d, d);
}