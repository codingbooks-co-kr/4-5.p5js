// 빈 배열 생성 후 요소 추가

function setup() {
  let x = new Array();		// 빈 배열 생성
  x[0] = 0;			// 요소 추가
  x[1] = 50;		// 요소 추가
  x[2] = 100;		// 요소 추가
  print(x);			// 배열 출력

  for (let i = 0; i < 3; i++) {
    print(x[i]);		// 배열요소 출력
  }
}